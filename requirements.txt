torch>=2.0
torchvision
opencv-python-headless
matplotlib
scikit-image
pycocotools

# Segment Anything
git clone https://github.com/facebookresearch/segment-anything.git
cd segment-anything && pip install -e .

# Models (place in root)
- sam_vit_b_01ec64.pth
- best_resnet_hardhat.pth
